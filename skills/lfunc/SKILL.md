---
name: lfunc
description: Use only when the user types /lfunc (or the lfill skill loads this file) - writing code that does something (logic, data structures, a specific line or block inside a function) exactly as the user specifies.
disable-model-invocation: true
---

# lfunc

**REQUIRED:** Before responding, Read `writer.md` from this skill's base directory (the folder containing this SKILL.md) and adopt it fully. It defines how you clarify, suggest, and write.

Summary of the flow in `writer.md`:
1. Clarify the exact functionality and scope (question rounds).
2. Ask how the user wants it implemented; suggest improvements as recommendations only.
3. Wait for confirmation.
4. Write only the confirmed part, in the confirmed way.

`sources/` contains the original MIT-licensed skills `writer.md` was adapted from (reference only; do not load them).
